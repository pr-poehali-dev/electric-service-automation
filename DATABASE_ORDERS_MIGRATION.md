# Миграция заявок из localStorage в PostgreSQL

## ✅ Что сделано

Заявки теперь автоматически сохраняются в базу данных PostgreSQL вместо localStorage. Это обеспечивает:

- ✅ **Надёжное хранение** - данные не теряются при очистке браузера
- ✅ **Синхронизация между устройствами** - доступ к заявкам с любого устройства
- ✅ **Двусторонняя синхронизация с Планфикс** - изменения в Планфиксе автоматически обновляют статус на сайте
- ✅ **Быстрый поиск и фильтрация** - индексы в БД для быстрого доступа
- ✅ **История изменений** - автоматическое отслеживание created_at и updated_at

## 🗄️ Структура базы данных

### Таблица: `orders`

**Основные поля:**
- `id` - внутренний ID в БД (автоинкремент)
- `order_uid` - уникальный ID заявки в формате `ORD-timestamp`
- `status` - статус заявки (new, confirmed, in_progress, completed)
- `planfix_task_id` - ID задачи в Планфиксе для синхронизации

**Контактные данные:**
- `customer_name` - имя клиента
- `customer_phone` - телефон клиента
- `customer_email` - email клиента (опционально)

**Данные заявки:**
- `items` - JSON массив услуг: `[{name, price, quantity, category, description}]`
- `address` - адрес выполнения работ
- `scheduled_date` - запланированная дата
- `scheduled_time` - запланированное время
- `total_price` - общая стоимость
- `total_switches` - количество выключателей
- `total_outlets` - количество розеток
- `total_points` - общее количество точек
- `estimated_cable` - примерное количество кабеля (метры)
- `estimated_frames` - примерное количество рамок

**Исполнитель:**
- `assigned_to` - UID исполнителя
- `assigned_to_name` - имя исполнителя

**Оплата:**
- `payment_status` - статус оплаты (unpaid, partially_paid, paid)
- `paid_amount` - оплаченная сумма
- `payments` - JSON массив платежей

**Метаданные:**
- `created_at` - дата создания
- `updated_at` - дата последнего обновления

### Индексы для быстрого поиска:
- `order_uid` - поиск по ID заявки
- `customer_phone` - поиск по телефону клиента
- `status` - фильтрация по статусу
- `assigned_to` - фильтрация по исполнителю
- `planfix_task_id` - синхронизация с Планфикс

## 🔄 API для работы с заявками

**Base URL:** `https://functions.poehali.dev/011a42c8-fcaa-413f-b611-d66cb669ba4e`

### Получить все заявки
```bash
GET /
GET /?status=new
GET /?assigned_to=user123
```

### Получить заявку по ID
```bash
GET /?id=ORD-1234567890
```

### Создать новую заявку
```bash
POST /
Content-Type: application/json

{
  "order_uid": "ORD-1234567890",
  "customer_name": "Иван Иванов",
  "customer_phone": "+79991234567",
  "address": "г. Калининград, ул. Ленина, д. 10",
  "scheduled_date": "2025-11-10",
  "scheduled_time": "14:00",
  "items": [
    {"name": "Установка розетки", "price": 500, "quantity": 2},
    {"name": "Установка выключателя", "price": 400, "quantity": 1}
  ],
  "total_price": 1400,
  "status": "new"
}
```

### Обновить заявку
```bash
PUT /?id=ORD-1234567890
Content-Type: application/json

{
  "status": "in_progress",
  "assigned_to": "user123",
  "assigned_to_name": "Мастер Иванов"
}
```

### Удалить заявку
```bash
DELETE /?id=ORD-1234567890
```

## 🔄 Как работает синхронизация

### 1. Создание заявки (Сайт → БД → Планфикс)

```
Клиент создаёт заявку
    ↓
Сохранение в БД (orders-api)
    ↓
Отправка в Планфикс (planfix)
    ↓
Создание задачи в Планфиксе
```

### 2. Изменение статуса (Планфикс → БД → Сайт)

```
Менеджер меняет статус в Планфиксе
    ↓
Webhook → planfix?webhook=true
    ↓
Обновление в БД (orders-api PUT)
    ↓
Автоматическое обновление на сайте при следующей загрузке
```

### 3. Назначение исполнителя (Сайт → БД)

```
Админ назначает мастера
    ↓
Обновление в БД (orders-api PUT)
    ↓
Синхронизация через localStorage для UI
```

## 📊 Автоматическая миграция

При первом запросе к сайту:

1. **Загрузка из БД** - система пытается загрузить заявки из PostgreSQL
2. **Fallback к localStorage** - если БД недоступна, используются данные из localStorage
3. **Автосохранение** - новые заявки автоматически сохраняются в обе системы

## 🔍 Статусы заявок

### В системе:
- `new` - Новая заявка
- `confirmed` - Подтверждена
- `in_progress` - В работе
- `completed` - Завершена
- `cancelled` - Отменена

### Соответствие Планфиксу:
| Планфикс | Система |
|----------|---------|
| Новая / Новое | new |
| Принято / Подтверждено | confirmed |
| В работе / Выполняется | in_progress |
| Завершена / Выполнена / Закрыта | completed |
| Отменена / Отменено | cancelled |

## 🚀 Преимущества новой системы

### Для клиентов:
- ✅ Надёжное сохранение заявок
- ✅ История всех заявок
- ✅ Актуальные статусы в реальном времени

### Для менеджеров:
- ✅ Единый источник данных
- ✅ Синхронизация с Планфиксом
- ✅ Быстрый поиск и фильтрация

### Для электриков:
- ✅ Доступ к заявкам с любого устройства
- ✅ Автообновление статусов
- ✅ История выполненных работ

## 📝 Миграция существующих данных

Существующие заявки из localStorage автоматически сохраняются в БД при первой загрузке после обновления. Ручная миграция не требуется.

---

**Версия:** 1.0  
**Дата:** 2025-11-06  
**Автор миграции:** V0004__add_order_fields_for_localStorage_data.sql
